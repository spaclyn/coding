from masterFunctions import buildProfile as bp


userProfile = bp.buildProfile('albert', 'einstein',
                           location = 'princeton',
                           field = 'physics')

print(userProfile)